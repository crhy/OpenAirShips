{
  "name": "openairships-site",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev web",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
